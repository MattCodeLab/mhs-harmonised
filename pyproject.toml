[project]
name = "mhs-automation-harmonised"
version = "1.0.0"
description = "BNM MHS Excel → DuckDB → harmonised data pipeline"
readme = "docs/README.md"
requires-python = ">=3.13"
dependencies = [
    "duckdb>=1.5.2",
    "openpyxl>=3.1.5",
    "pandas>=3.0.2",
    "pyarrow>=24.0.0",
    "xlrd>=2.0.2",
]
